# Changelog

## v1.5.3 - 2026-09-11

### 新增与优化

- **DSH CLI 多渠道版本动态拉取与一键安装**：
  - 支持从 npm registry 与国内镜像动态解析 DSH 的全量分发标签（`latest`、`next`、`alpha`、`beta` 等）及历史发布版本，不再写死单一 latest 版本；
  - 版本下拉框中清晰呈现版本号、渠道 Tag（如 `next`、`alpha`）以及精确发布日期（`YYYY-MM-DD`）；
  - **支持一键执行全局安装**：外部环境无需再手动复制命令到终端运行，可直接在版本弹窗点击「一键执行安装」，由后台通过用户 Shell 环境自动执行全局更新，实时将终端日志推送到服务日志面板；托管环境亦支持一键升级至指定选定版本。
- **插件在线安装与精准版本切换（专属弹窗）**：
  - **新插件在线安装**：插件管理弹窗新增安装栏，输入插件包名即可实时查询其可用版本与渠道 tags，下拉选择目标版本一键安装；
  - **插件切换版本专属弹窗**：彻底淘汰行内嵌入式下拉框，点击「切换版本」弹出独立的模态弹窗，清晰展示插件名称、当前版本、目标版本选择与发布日期，并提供「安装并切换」主按钮，消除行内元素溢出与按钮挤压变形；
  - **长版本号自动省略截断**：针对本地路径（如 `file:/.../xxx.tgz`）或较长版本号增加文本省略（`ellipsis`）与完整路径 hover 提示，确保插件列表在各种窗口尺寸下均保持优雅规整。

## v1.5.2 - 2026-09-06

### 缺陷修复

- **修复 macOS 触控板轻触后卡在按住拖拽/划选状态的问题**：
  - 在 macOS 平台下，WebKit/WKWebView 在触控板轻触（Tap to click）或开启三指拖移时偶发出现仅派发 `mousedown`/`pointerdown` 但抬手后遗漏 `mouseup`/`pointerup` 的底层缺陷，导致光标移动被页面误判定为持续按住拖拽或文本划选；
  - 注入 macOS 全局释放守卫脚本（`MACOS_STALE_RELEASE_GUARD_SCRIPT`），在捕获阶段实时跟踪指针移动；一旦检测到此前处于按下但物理按键已全部松开（`e.buttons === 0`），立即拦截该帧移动并向当前目标派发合成的 `pointerup` 与 `mouseup` 事件，通知各层级组件清理拖拽状态机；
  - 全面覆盖主窗口标题栏、内嵌 DSH 服务子 WebView 以及独立窗口，并在窗口失焦（`blur`）、页面隐藏与系统原生拖拽结束时自动兜底重置。

## v1.5.1 - 2026-09-06

### 新增与优化

- **插件管理功能与交互体验升级**：
  - **安装时间显示与倒序排列**：后端自动探测 `node_modules` 下插件目录、符号链接或 `package.json` 的元数据提取安装时间戳（`installedAt`）；插件列表默认按安装时间倒序排列（最新安装排在最前），无安装时间项优雅降级置底；前端列表项展示格式化安装日期，支持 hover 查看完整时间；
  - **支持插件关键词搜索**：弹窗顶部新增搜索栏，支持按插件名称及版本即时过滤匹配，并提供快捷清空搜索按钮与未匹配空状态；
  - **固定列表高度与排版防抖**：将插件列表高度固定为 320px，统一在容器内居中展示加载中、空列表、搜索无匹配等状态提示，消除列表过滤与加载切换时的弹窗高度抖动；
  - **中英文多语言与深浅主题适配**：新增搜索与安装时间等文案已同步录入中英文字典，全面适配系统及应用深色主题样式；
  - **插件命令执行环境强化**：在插件安装与卸载等命令（`run_pnpm`）中注入启动器完整环境变量，确保在各类系统环境及自定义 PATH 下均能精准调用 `pnpm` / `node`。

## v1.5.0 - 2026-09-05

### 重大升级与重命名

- **项目全面升级更名为 DSH Desktop**：
  - 项目名称由 `dsh-launcher` 正式更名为 `dsh-desktop`，应用产品名称升级为 **DSH Desktop**；
  - 标识符同步升级为 `ai.deepseek.dsh-desktop`，更新发布源地址与 GitHub 仓库无缝迁移至 `jockiller/dsh-desktop`；
  - 优化全平台打包产物命名规范（统一为 `DSH_Desktop_*`）；
  - 运行时标记与本地存储全面平滑兼容迁移，保证已有环境无缝衔接。

## v1.4.1 - 2026-09-05

### 新增与优化

- **消除 WebView 与主窗口视口滚动回弹（Rubber-banding / Bounce）**：
  - 注入 `DISABLE_BOUNCE_SCRIPT` 脚本，将页面 `html` 与 `body` 的 `overscroll-behavior` 强制设置为 `none !important`；
  - 消除 macOS / Windows / Linux 各端在触控板或滚轮滚动到边界时的弹性拉伸与晃动，还原纯正桌面原生质感；
  - 保持长对话、代码块等页面内部元素顺滑滚动不受任何影响；
  - 主窗口前端骨架样式同步增加 `overscroll-behavior: none`。
- **规范前端包管理器声明**：
  - 在 `package.json` 中增加标准 `packageManager` 声明（`pnpm@10.5.2`），避免 IntelliJ IDEA / WebStorm 等 IDE 误提示 `npm install`。

## v1.4.0 - 2026-09-05

### 新增与优化

- **单窗口多 WebView 架构集成（一体化桌面体验）**：
  - 彻底淘汰原有的多窗口分离模式，将 DSH 内容 WebView 无缝挂载为主窗口子视图；
  - 顶部集成一体化自定义标题栏，居中实时同步展示 DSH 内容页的真实 `document.title`；
  - 运行状态胶囊与右上角齿轮设置按钮均支持在控制台主面板与 DSH WebView 之间一键平滑切换；
  - 任何情况下点击「打开」均可可靠切入 DSH 内容视图。
- **双向深浅色主题无缝同步**：
  - 启动器默认遵循操作系统深浅色偏好，彻底消除冷启动时的白屏闪烁；
  - 精确解析 `~/.dsh/settings.yaml` 偏好配置，支持在 DSH 设置为「跟随系统」时启动器自动保持系统主题同步；
  - 基于 URL Fragment 零侵入通信机制实时捕获 DSH 页面内主题变动并联动主窗口标题栏。
- **系统托盘图标工作状态动态呼吸指示**：
  - 状态栏托盘图标重构为适中的黄金比例尺寸，兼顾原生美感与视觉辨识度；
  - 动态监听后台会话（Session）任务执行状态，当后台存在工作中的会话时在托盘图标右上角以平滑抗锯齿小绿点动态闪烁；
  - 停用旧版 Dock 动态渲染，macOS Dock 图标恢复为系统默认静态图标。
- **启动时序优化与平滑延时揭示**：
  - 启动阶段延长主窗口 Loading 呈现时间，冷启动开启自动启动时连贯展示状态，不再中途闪烁暴露控制台底板；
  - 在 bootstrap 阶段即刻提前触发服务启动与 WebView 后台加载，为页面渲染争取先机；
  - 引入 800ms 延时平滑揭示机制（Reveal Delay），确保 WebView 内部首屏组件、样式及主题完全渲染后再行展现；
  - 修复冷启动在特定时序下未触发自动启动留在主界面的问题。

## v1.3.8 - 2026-09-04

### 新增与优化

- **黑白模版系统托盘（System Tray）**：
  - 增加常驻状态栏托盘，采用标准单色模版图标（Template Image），自适应系统明暗主题；
  - 托盘提供快捷操作菜单：打开主界面、打开 Web GUI、启动/停止/重启服务、完全退出；
  - 左键单击托盘图标或点击 Dock 图标随时唤起主窗口。
- **一键打开 Profile 目录**：
  - 设置面板 Profile 字段旁新增快捷文件夹按钮，一键在系统文件管理器中打开配置与插件目录。
- **插件重启等待弹窗支持手动停止**：
  - DSH 插件热重启过渡弹窗中补充「停止服务」按钮，用户可随时取消等待并回到主界面手动操作。
- **托盘快捷启动联动外部服务强停**：
  - 托盘快捷启动遇端口冲突时自动唤起主窗口，并同步呈现「强制停止外部服务」操作入口。
- **Web GUI 显式打开回退机制**：
  - 修复启动后动作设为「不执行」时，手动点击「打开 Web GUI」自动回退使用默认浏览器打开。
- **消除关窗退出卡顿与图标闪现**：
  - 关窗平滑隐藏，彻底移除关闭全部窗口的退出检测；
  - 增加 Dock 动画线程主动退出控制，消除应用退出时 Dock 图标短暂回退为终端图标的问题。

## v1.3.7 - 2026-09-02

### 修复

- **关闭全部窗口后应用退出**：
  - 任一窗口仍显示时保持运行（先关 Launcher 或先关内置 WebView 均可）；
  - Launcher 与内置 WebView 都不显示时退出并停止 DSH；
  - 三平台同一套逻辑，修复 macOS 关窗后仍留在 Dock 的问题。

## v1.3.6 - 2026-09-02

### 新增与优化

- **点击版本号弹窗查看更新内容**：
  - 点击右下角版本号始终打开版本对话框，展示当前版本更新说明；
  - 弹窗底部提供「跳转到 GitHub」与「检查更新」按钮；
  - 发现新版本时仍显示小红点，并在弹窗内展示最新版更新内容与应用内更新入口；
  - 启动时仍自动检查更新。

## v1.3.5 - 2026-09-02

### 新增与优化

- **热重启接管后自动恢复会话状态监视与 Cookie 复用**：
  - 内存凭据模型（`SessionCredentials`）：保存会话的 `origin`、`token` 及已换取的 `cookie`；
  - 启动与恢复监视时优先直接复用已有会话 Cookie 轮询 `/api/session/list`，避免重启后重复请求换发接口；
  - DSH 插件热重启被自动接管（Handoff）后，重新附着会话监视线程，macOS Dock 图标无缝恢复会话繁忙/空闲（绿⇄红）指示；
  - 完善故障隔离：会话监视作为纯外挂轻量只读观察者，全链路受 `catch_unwind` 保护，任何异常均静默安全退出并输出单条日志，绝对不影响主进程与 DSH 核心服务；
  - 代际取消管理：强化后台接管线程与前台生命周期（启动/停止/重启）的取消槽位联动，杜绝孤儿线程。

## v1.3.4 - 2026-09-02

### 新增

- **DSH 插件/内部热重启接管机制（Process Handoff / Adoption）**：
  - 感知 DSH 插件热重启（detached restart）时的正常退出信号，自动进入 `restarting` 重启过渡状态；
  - 自动寻找同端口新进程并进行指纹参数（`--profile`、`--host`、`--port`、`custom_args`）与 HTTP 健康检查校验，通过后无缝收养（Adopt）；
  - 维持内置 Webview 窗口持续可用，彻底消除热重载时窗口被误关或状态闪退的问题。
- **全局操作遮罩与居中状态反馈卡片**：
  - 点击「启动」、「停止」、「重启」以及插件热重启过渡期，自动呼出全屏半透明遮罩与居中加载反馈卡片，避免因异步等待导致的重复点击；
  - 采用 `flushSync` 强制同步渲染与 `requestAnimationFrame` 保证在发起后端 IPC 前遮罩立即物理绘制上屏；
  - 遮罩卡片内支持在启动阶段直接点击「停止服务」快速取消。

### 优化

- 补充 Rust 后端 `stop` 流程的 `stopping` 状态实时广播与并发代际（`generation`）控制。
- 完善中英文双语字典，适配亮色与暗色模式下的交互样式与遮罩毛玻璃效果。

## v1.3.3 - 2026-09-01

### 新增

- macOS Dock 图标三态指示（仅 macOS，Windows/Linux 保持静态原图标）：
  - 服务未启动 / 已停止 / 启动失败：电源按钮红色常亮
  - 服务运行中、无会话：电源按钮绿色常亮
  - 有会话执行任务：绿⇄红交替闪烁（0.6s/周期），结束后回到绿色

### 修复

- 有会话时图标停在红色不闪烁：Busy 状态每帧都会翻转，不再被静态去重误杀。
- 停止服务后旧监视线程延迟写入不再覆盖红色 Idle。
- Dock 动态图标留白按实测校准到与邻居图标一致（CONTENT_SCALE 0.89）。

### 兼容

- 监视器仍为纯外挂：独立线程 + panic 兜底，失败最多一条日志，不影响启动/停止。
- Windows / Linux 无 Dock 改动；AppKit 依赖仅在 macOS 编译。

## v1.3.2 - 2026-08-31

### 修复

- 闪烁结束后图标可能停留在红色状态：现以 ≤1.3s 恢复为**系统包内原始图标**（此前恢复的是缩放后的绿色帧，且最长 2.3s 延迟）。
- 会话轮询间隔 2s → 1s，任务结束时的指示收尾更即时。

## v1.3.1 - 2026-08-31

### 修复

- Dock 图标留白比例校准：上一版补足留白时双重缩减，导致图标比邻居小一圈；现按 artwork 内容占比 0.92 → Apple 栅格目标 0.805 的精确比例（0.87）缩放，与系统图标视觉一致。

## v1.3.0 - 2026-08-31

### 新增

- 检测 DSH 运行中会话并在 Dock 图标上呈现工作状态闪烁：有会话执行任务时，应用图标右下角电源按钮以绿⇄红两态交替闪烁（0.6s/周期），任务结束自动恢复原始绿色图标（仅 macOS，其他平台不受影响）。
- 图标按 Apple 图标栅格补足标准留白，修正 Dock 内显示比其他图标偏大的问题。
- 指示通过 dsh 启动日志捕获的 launch token 在内存中换取会话 cookie，并轮询 `/api/session/list` 统计运行中的主会话与子代理会话；token 不落盘，仅保存在内存。

### 兼容

- 监视器为纯外挂观察者：独立线程 + panic 兜底 + 静默退出，任何失败（无 token、cookie 失效、DSH 重启、网络异常）最多输出一条日志，绝不影响服务的启动/停止与现有功能。
- 无 token 的旧版 DSH 会自动跳过会话监视（仅提示一次）；DSH 输出格式变化时逐项容错跳过，不会崩溃。

### 其他

- 外部服务提示文案精简（移除"您仍可以直接打开 Web GUI 使用该服务"）。

## v1.2.3 - 2026-08-31

### 修复

- 打开 Web GUI 优先使用 dsh 启动日志捕获的带 token URL，健康检查通过后限时等待日志解析，修复打开固定地址提示 "authentication required" 的问题。
- 内置 WebView 窗口复用时自动导航到本次启动的新地址，服务重启后不再停留在旧页面。
- 移除启动按钮上方的多余链接与 PATH 诊断日志。

### 兼容

- 已适配 DSH v0.1.2 的带 token URL 格式，浏览器首次打开即自动完成登录换发，无 token 的旧地址不再可用。

### 其他

- 应用启动时首帧前同步主题，修复暗色主题下窗口先白后暗的闪烁。

## v1.2.2 - 2026-08-31

### 修复

- 自动检测、手动选择 DSH 和启动时的版本探测改为复用已缓存的登录 Shell 工具链环境，修复 nvm 安装的 `dsh` 因找不到 `node` 报 `env: node: No such file or directory`。
- 进入 Launcher 时显示全局加载提示，至少停留片刻；勾选自动启动时先显示“正在自动启动...”，避免主界面一闪而过。

## v1.2.1 - 2026-08-31

### 修复

- 修复通过 Launcher 启动 DSH 时继承 GUI 会话中的错误 Node/pnpm PATH，导致插件安装出现 `ERR_PNPM_UNEXPECTED_STORE`。
- 正式启动服务时从用户默认登录 Shell 读取工具链环境（PATH、PNPM_HOME、nvm/volta/asdf/mise/fnm），不再把 GUI 旧 PATH 传给 Shell 初始化。
- 登录 Shell 使用平台最小系统 PATH 启动，保证 rc 文件在设置用户 PATH 前仍能调用基础命令。
- 外部 DSH 只追加自身目录，不改变用户工具链优先级；托管 DSH 仅在规范入口、有效标记和完整 Node/runtime 同时存在时前置托管运行时。
- 兼容 Windows 环境变量键的大小写语义，并保留 Unix 环境变量中的非 UTF-8 字节。
- 版本探测不再重复启动登录 Shell，避免 bootstrap/detect/validate 额外卡住。

### 验证

- macOS Rust 单元测试通过。
- Windows GNU 目标 Rust 编译检查通过。
- Linux 原生构建由 GitHub Actions runner 执行。

## v1.2.0 - 2026-08-31

- 外部 DSH 强制关闭与版本信息弹窗。

## v1.1.0 - 2026-08-31

- 增加 DSH 版本更新提示与升级命令弹窗。
